
info中的命令：可以在info中使用 ? 来查看

n 和 p :  下/上一节
空格 和 回车: 向后/前移动
[ 和 ] : 类似n和p，但是可以在父子节点移动
m : 提示输入菜单项（本节点中的），可以用tab补全，输入完成后回车则跳转到输入的项对应的那一节
	菜单: 一般以  一行单独的  *menu  开始，每一个项之前有一个 *
ctrl + g : 取消一个命令，例如m在输入菜单项时
f : 提示输入引用的主题名称（在当前节点内的），然后跳转到该主题
f? : 显示该节点内所有引用的主题
	交叉引用： 一般格式 *Note XXX： XXXXX
l : 返回上一步所在节点(info保存访问记录，可以通过l一次返回遍历)



